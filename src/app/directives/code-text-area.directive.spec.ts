import { CodeTextAreaDirective } from './code-text-area.directive';

describe('CodeTextAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeTextAreaDirective();
    expect(directive).toBeTruthy();
  });
});
